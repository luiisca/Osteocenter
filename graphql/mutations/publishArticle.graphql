mutation PublishArticle($id: ID) {
  publishArticle(where: {
    id: $id
  }) {
    id
    slug
    title
    excerpt
    content {
      raw
      markdown
      html
    }
    featuredPost
    featuredImage {
      width
      height
      url
    }
  }
}
