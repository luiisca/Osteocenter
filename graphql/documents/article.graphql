query Article($slug: String!) {
  article(where: {slug: $slug}) {
    slug
    publishedAt
    featuredImage {
      url
        height
        width
    }
    title
    excerpt
    categories {
      name
      slug
    }
    content {
      html
    }
  }
}

