query Article($slug: String!) {
  article(where: {slug: $slug}) {
    id
    slug
    title
    excerpt
    content {
      raw
      markdown
      html
    }
    featuredPost
    featuredImage {
      width
      height
      url
    }
  }
}

