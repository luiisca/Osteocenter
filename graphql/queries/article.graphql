query Article($slug: String!) {
  article(where: {slug: $slug}) {
    id
    slug
    title
    excerpt
    publishedAt
    content {
      raw
      markdown
      html
    }
    featuredPost
    featuredImage {
      width
      height
      url
    }
    categories {
      name
      slug
    }
  }
}

